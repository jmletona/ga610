import * as internal from 'stream';

export interface Person {
  id: number;
  active_status: number;
  name: String;
  lastName: String;
  company: String;
  description: String;
  address: String;
}
